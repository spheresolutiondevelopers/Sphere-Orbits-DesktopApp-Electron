{
  "name": "sphere-orbits-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Sphere Schedule – Cross-platform desktop app (Electron)",
  "main": "packages/main/dist/index.js",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "start": "electron packages/main/dist/index.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.13.3",
    "typescript": "^5.4.5",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}